# Supabase (create a project at supabase.com)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Required: used server-only to create/update the single login account
# (see "Login" section in the README) — never exposed to the client.
SUPABASE_SERVICE_ROLE_KEY=

# Anthropic API (for live ATO guidance lookups per session)
ANTHROPIC_API_KEY=
